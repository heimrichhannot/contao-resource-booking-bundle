{% trans_default_domain 'huh_rb' %}
<div class="blob-widget">
    {% if data == null %}
        <p>Keine Daten.</p>
    {% else %}
        <table>
            <thead>
                <tr>
                    <th>Datum</th>
                    <th>Wert</th>
                </tr>
            </thead>
            <tbody>
            {% for key, value in data %}
                <tr>
                    <td>{{ ('booking_data.' ~ key)|trans }}</td>
                    {% set value_trans_key = ('booking_data_value.' ~ key ~ '.' ~ value) %}
                    {% if ' ' in value %}
                        <td>{{ value }}</td>
                    {% else %}
                        {% set value_translated = value_trans_key|trans %}
                        {% if value_translated != value_trans_key %}
                            <td>{{ value_translated }}</td>
                        {% else %}
                            <td>{{ value }}</td>
                        {% endif %}
                    {% endif %}
                </tr>
            {% endfor %}
            </tbody>
        </table>
    {% endif %}
</div>
<style>
    .blob-widget {
        margin-top: .5rem;

        table {
            width: 100%;
            border-collapse: collapse;
            border-spacing: 0;
            th, td {
                padding: .5rem .75rem;
            }

            tbody tr:nth-child(odd) {
                background-color: #aaaaaa22;
            }
        }
    }
</style>