/* empty css                                                         */
import "../_virtual/air-datepicker-DlRCcePd.js";
class i {
  /**
   * @param {HTMLElement} $root
   * @param {BookingFormConfig} config
   */
  constructor(t, o) {
    if (this.$root = t, this.config = o, this.$form = t.querySelector(o.selectors.form), this.$mount = t.querySelector(o.selectors.mount), !this.$form) throw new Error(`Form not found via selector "${o.selectors.form}"`);
    if (!this.$mount) throw new Error(`Mount not found via selector "${o.selectors.mount}"`);
  }
  init() {
  }
}
export {
  i as default
};
